<h1><%= @project.title %></h1>

<p><%= @project.description %></p>

<ul>
  <li><%= link_to "Edit", edit_project_path(@project) %></li>
  <li><%= link_to "Delete", project_path(@project),
                  method: :delete,
                  data: { confirm: "Confirm" } %></li>
</ul>
